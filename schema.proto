syntax = "proto3";
package proto;

message Address {
    string city = 1;
}

message Merchant {
    int64 id = 1;
    string name = 2;
    float fraud_rate = 3;
    bool active = 4;
    int64 balance = 5;
    Address address = 6;
    repeated int64 tags = 7;
    repeated string products = 8;
}
